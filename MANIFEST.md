# Patch Manifest

The installer copies every `.lang` file under `patch/` to the same relative path
inside the SOMA installation directory.

Default SOMA directory:

```text
/Applications/SOMA
```

Backup directory:

```text
$HOME/Library/Application Support/SOMA-TR-Yama/backups
```
